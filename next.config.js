/** @type {import('next').NextConfig} */
const nextConfig = {
    //output: 'export',
    basePath: '/glauciocesar',
    assetPrefix: '/glauciocesar',
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig;
